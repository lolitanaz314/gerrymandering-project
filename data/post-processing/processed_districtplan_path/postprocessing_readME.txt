###

The directories that end in "percents" contain:

>> JSONs for the MGGG-GENERATED district plans (for each state)
	>> measures data for district plans
	>> sorted percentages for every category across all districts () for every district plan
	>> seat vote share input for the graphs

###

The JSON files "districtPlan_w_geometry" contain no measure/percentage information, USE FOR districtPlan GEOMETRIES

Each directory will have 10,000 files (one for each distrcitPlan)